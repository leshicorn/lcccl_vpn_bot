{
  "entities": {
    "UserMapping": {
      "title": "User Mapping",
      "description": "Maps a nickname to a Telegram user ID.",
      "type": "object",
      "properties": {
        "nickname": { "type": "string", "description": "The unique nickname of the family member." },
        "telegramId": { "type": "integer", "description": "The Telegram user ID of the family member." }
      },
      "required": ["nickname", "telegramId"]
    },
    "VpnConfig": {
      "title": "VPN Configuration",
      "description": "Stores the VPN configuration file content.",
      "type": "object",
      "properties": {
        "nickname": { "type": "string", "description": "The owner's nickname." },
        "device": { "type": "string", "description": "The device name." },
        "content": { "type": "string", "description": "The content of the .conf file." },
        "fileName": { "type": "string", "description": "The original filename." },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["nickname", "device", "content", "updatedAt"]
    }
  },
  "firestore": {
    "/mappings/{nickname}": {
      "schema": "UserMapping",
      "description": "Collection of user nickname to Telegram ID mappings."
    },
    "/configs/{configId}": {
      "schema": "VpnConfig",
      "description": "Collection of VPN configurations."
    }
  }
}
